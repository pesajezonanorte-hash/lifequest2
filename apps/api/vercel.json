{
  "version": 2,
  "buildCommand": "npm run build --workspace=@lifequest/api && npx prisma generate && npx prisma migrate deploy",
  "builds": [
    {
      "src": "src/server.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/server.ts"
    }
  ]
}
