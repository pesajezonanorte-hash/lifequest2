{
  "version": 2,
  "buildCommand": "npx prisma migrate deploy && npm run build --workspace=@lifequest/api && npx prisma generate",
  "builds": [
    {
      "src": "src/server.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/server.ts"
    }
  ]
}
